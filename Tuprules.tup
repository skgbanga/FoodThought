.gitignore

CFLAGS += -pedantic
CFLAGS += -std=c++14
CFLAGS += -Wall
CFLAGS += -I/Users/Sandeep/Documents/projects/FoodThought/

# command to create object files
!cc = |> clang++ $(CFLAGS) -c %f -o %o |> %B.o
